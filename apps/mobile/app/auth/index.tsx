import { SignInScreen } from "../../src/features/auth/SignInScreen";

/**
 * Renders the authentication entry point for the mobile app.
 *
 * @returns A JSX element that displays the sign-in screen.
 */
export default function AuthIndex() {
  return <SignInScreen />;
}